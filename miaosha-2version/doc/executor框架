excutor框架是java提供的一个多线程管理、使用的解决方案。
其核心是excutor框架会调用execute()方法执行任务，并通过一下规则判断是否创建新线程来执行
1 如果未达到核心线程数，则创建新线程
2 如果阻塞对列未满，则加入阻塞对列
3 如果未达到非核心线程数，则创建新非核心线程
在创建新线程执行的时候，会调用runworker方法。该方法有一个while循环，在第一次进入循环的
时候，会执行worker中的firstask任务，之后会从阻塞对列workqueue中获取任务。如果拿不到，那么
该方法会一直阻塞下去，直到拿到任务为止。在此期间任务线程一直处于运行状态，没有销毁。因此当拿到新任务
后，该线程继续执行任务。这个过程中避免了线程频繁创建和销毁的开销，达到了线程复用的目的。

