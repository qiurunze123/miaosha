可重入锁
1 实现lock接口，继承AQS同步器
2 可中断、可重入
3 支持非公平锁和公平锁，默认支持非公平锁，内含sync父类，实现fairsync和nofairsync子类
非公平锁：各线程之间共同竞争锁资源，就是在在等待对列中的head节点的next节点会和新来的线程节点竞争同步状态
公平锁：就是按照先到先得的原则去获取锁资源。
4 主要原理
 当线程节点初次获取同步状态之后，就会将state状态置为1。其它未获取同步状态的线程节点，默认会进入到等待对列中，并阻塞，等待唤醒排队获取同步状态；
 如果该节点再次获取同步状态之后，就会将state值加1；如果拥有同步状态的线程释放锁资源，就将state值减1，并唤醒等待对列中head节点的next节点线程
 去重新获取同步状态，当state的值为0时，标识没有线程节点获得同步状态，那么其它线程就可以获取同步状态了。
5 实现tryaccquire，tryrelease方法

